<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.docmall.demo.mapper.PracticeMapper">

	
	<insert id="write" parameterType="com.docmall.demo.domain.PracticeVO"  >
		INSERT INTO 
			practice(bno, title, CONTENT, writer) 
		VALUES
			(SEQ_PRACTICE.NEXTVAL, #{title}, #{content}, #{writer});	
	</insert>


	<select id="list" resultType="com.docmall.demo.domain.PracticeVO">
		SELECT
		    bno, title, content, writer, regdate, updatedate, viewcount
		FROM
		    practice
		ORDER BY
		 	bno DESC
	</select>








</mapper>

